<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shop.mapper.OrderItemMapper">

    <!-- 根据订单ID查询订单商品列表 -->
    <select id="selectByOrderId" resultType="com.example.shop.entity.OrderItem">
        SELECT * FROM order_item 
        WHERE order_id = #{orderId}
        ORDER BY item_id ASC
    </select>

    <!-- 根据订单编号查询订单商品列表 -->
    <select id="selectByOrderNo" resultType="com.example.shop.entity.OrderItem">
        SELECT * FROM order_item 
        WHERE order_no = #{orderNo}
        ORDER BY item_id ASC
    </select>

    <!-- 批量插入订单商品 -->
    <insert id="insertBatch">
        INSERT INTO order_item (
            order_id, order_no, product_id, product_name, product_image,
            current_price, quantity, total_price
        ) VALUES
        <foreach collection="items" item="item" separator=",">
            (
                #{item.orderId}, #{item.orderNo}, #{item.productId}, 
                #{item.productName}, #{item.productImage},
                #{item.currentPrice}, #{item.quantity}, #{item.totalPrice}
            )
        </foreach>
    </insert>

    <!-- 根据订单ID删除订单商品 -->
    <delete id="deleteByOrderId">
        DELETE FROM order_item WHERE order_id = #{orderId}
    </delete>

    <!-- 获取订单商品数量 -->
    <select id="countByOrderId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM order_item WHERE order_id = #{orderId}
    </select>

</mapper>
