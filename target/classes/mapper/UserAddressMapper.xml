<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shop.mapper.UserAddressMapper">

    <!-- 根据用户ID查询地址列表 -->
    <select id="selectByUserId" resultType="com.example.shop.entity.UserAddress">
        SELECT * FROM user_address WHERE user_id = #{userId} ORDER BY is_default DESC, address_id DESC
    </select>

    <!-- 根据用户ID查询默认地址 -->
    <select id="selectDefaultByUserId" resultType="com.example.shop.entity.UserAddress">
        SELECT * FROM user_address WHERE user_id = #{userId} AND is_default = 1 LIMIT 1
    </select>

    <!-- 清除用户的所有默认地址 -->
    <update id="clearDefaultByUserId">
        UPDATE user_address SET is_default = 0 WHERE user_id = #{userId}
    </update>

    <!-- 设置默认地址 -->
    <update id="setDefaultAddress">
        UPDATE user_address SET is_default = 1 WHERE address_id = #{addressId} AND user_id = #{userId}
    </update>

</mapper>
